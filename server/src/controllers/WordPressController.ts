import {Router} from "express"
import {connection} from '../config/db'

const wordPressController: Router = Router()

wordPressController.get('/:id', (res, req) => {

})

wordPressController.get('/all', (res, req) => {

})

wordPressController.post('/', (res, req) => {

})